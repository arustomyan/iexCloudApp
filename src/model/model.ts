export type dataQuotes = {
  askPrice: number;
  askSize: number;
  bidPrice: number;
  bidSize: number;
  lastSalePrice: number;
  lastSaleSize: number;
  // lastSaleTime: Date;
  // lastUpdated: Date;
  sector: string;
  securityType: string;
  symbol: string;
  volume: number;
};
